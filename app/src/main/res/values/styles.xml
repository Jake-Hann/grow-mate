<resources>

    <!-- Base application theme -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/cadetBlue</item>
        <item name="colorPrimaryDark">@color/blueBlack</item>
        <item name="colorAccent">@color/slate</item>
    </style>

    <!-- Toolbar theme -->
    <style name="CustomToolBarStyle" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:background">@drawable/toolbar_background</item>
        <item name="titleTextColor">@color/slate</item>
        <item name="android:tint">@color/slate</item>
    </style>

    <!-- Activity heading theme -->
    <style name="ActivityHeadings" parent="android:Widget.TextView">>
        <item name="android:textColor">@color/cadetBlue</item>
        <item name="android:textSize">34sp</item>
    </style>

    <!-- User hints theme -->
    <style name="UserHints" parent="android:Widget.TextView">>
        <item name="android:textColor">@color/cadetBlue</item>
        <item name="android:textSize">15sp</item>
    </style>

    <!-- User hints theme -->
    <style name="UserHintsSmall" parent="android:Widget.TextView">>
        <item name="android:textColor">@color/cadetBlue</item>
        <item name="android:textSize">14sp</item>
    </style>

    <!-- Toolbar dropdown menu theme -->
    <style name="CustomPopupStyle" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:background">@color/rain</item>
        <item name="android:textColor">@color/backgroundGrey</item>
        <item name="actionMenuTextColor">@color/backgroundGrey</item>
        <item name="overlapAnchor">false</item>
        <item name="android:dropDownVerticalOffset">-4dp</item>
    </style>

    <!-- Button style -->
    <style name="ButtonStyle" parent="@android:style/Widget.Button">
        <item name="android:layout_width">250dp</item>
        <item name="android:layout_height">50dp</item>
        <item name="android:textColor">@color/slate</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">bold</item>
    </style>


    <!-- Main Menu Button style -->
    <style name="MainMenuButtonStyle" parent="@android:style/Widget.Button">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">50dp</item>
        <item name="android:textColor">@color/slate</item>
        <item name="android:textSize">18sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- Plant today button style -->
    <style name="PlantTodayButtonStyle" parent="@android:style/Widget.Button">
        <item name="android:layout_width">200dp</item>
        <item name="android:layout_height">54dp</item>
        <item name="android:textColor">@color/cadetBlue</item>
        <item name="android:textSize">15sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- Add to My Plants style-->
    <style name="AddToMyPlantsStyle" parent="android:Widget.TextView">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@color/cadetBlue</item>
        <item name="android:textSize">18sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- Login EditText style -->
    <style name="LoginEditTextStyle" parent="android:Widget.EditText">
        <item name="android:layout_width">250dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/rain</item>
        <item name="android:hint">@color/backgroundGrey</item>
    </style>

</resources>
